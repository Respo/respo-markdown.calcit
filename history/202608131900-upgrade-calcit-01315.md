# Respo Markdown Calcit 0.13.15 migration

- Started from the latest remote `main` after fetching all branches.
- Upgraded Calcit and `@calcit/procs` to 0.13.15, with Respo/UI modules at
  0.16.69/0.7.7.
- Kept the canonical `calcit.cirru` snapshot and existing typed incremental
  parser state boundary.
- Added a CI deprecation gate so removed APIs cannot return unnoticed.
- Documented the required generation order: smoke/performance entries first,
  then the default entry before Vite bundling, preventing stale entry output.

Validation: MathML smoke, incremental parser performance, `cr --check-only`,
default `cr js`, deprecated API analysis (zero calls), immutable Yarn install,
and Vite build all pass locally. Existing Dynamic slots remain documented at
the parser/Respo boundary and are not hidden by warning suppression.
