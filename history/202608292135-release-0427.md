# Release 0.4.27

- Publish the dependency-cycle cleanup that keeps respo-markdown depending on respo-ui while removing the reverse dependency from respo-ui.
- Upgrade the supported Calcit toolchain to 0.13.63 and respo-ui to 0.7.12.
- Enforce strict dependency resolution and remove the remaining deprecated `tag-match` usages.
