# Upgrade UI 0.7.12 and Calcit 0.13.63 / 升级 UI 0.7.12 与 Calcit 0.13.63

- Upgraded to the cycle-free respo-ui 0.7.12 graph and enabled `caps --strict --ci` in Actions.
- 升级到无循环依赖的 respo-ui 0.7.12 依赖图，并在 Actions 启用 `caps --strict --ci`。
- Upgraded Calcit/@calcit/procs together to 0.13.63 and canonically formatted the Snapshot.
- 将 Calcit/@calcit/procs 同步升级到 0.13.63，并规范化 Snapshot。
- Migrated three deprecated `tag-match` calls to `match`.
- 将三处弃用的 `tag-match` 迁移为 `match`。
- MathML smoke, incremental parser benchmark, 2/2 definition tests, baseline/deprecated gates, JS codegen, and Node 24/Vite 8 build pass.
- MathML smoke、增量解析 benchmark、2/2 定义测试、基线/弃用守门、JS codegen 与 Node 24/Vite 8 构建均通过。
